# Attack Fixtures

The executable fixtures live in `examples/violations/` and
`examples/allowed/`. This directory exists as the scenario index for live demos.

Run:

```bash
make test-policies
```

Then open:

```text
docs/ATTACK-SCENARIOS.md
reports/evidence.md
```

after running:

```bash
make evidence
```
